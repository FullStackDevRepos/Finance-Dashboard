import { Box } from "@mui/material";
import styled from "@emotion/styled";

const FlextBetween = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center"
})

export default FlextBetween;